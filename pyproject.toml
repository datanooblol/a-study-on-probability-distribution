[project]
name = "a-study-on-probability-distribution"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/my_modules"]
